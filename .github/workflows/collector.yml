name: Run Job Collector

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  build-and-commit:
    permissions:
      contents: write
      
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml httpx python-dotenv beautifulsoup4 lxml spacy langdetect numpy playwright
          python -m spacy download fr_core_news_md
          python -m spacy download en_core_web_md
          python -m playwright install --with-deps
          
      - name: Run collector script and copy database
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python collector.py
          cp storage/jobs.db ui/public/jobs.db

      # --- 👇 LA NOUVELLE STRATÉGIE 👇 ---
      - name: Generate timestamp file
        env:
          # On définit le fuseau horaire pour la commande 'date'
          TZ: 'Europe/Paris'
        run: |
          # On crée le fichier qui contient la date et l'heure (maintenant heure de Paris)
          date +"%d/%m/%Y %H:%M" > ui/public/last-update.txt

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # On ajoute tous les fichiers modifiés (la DB et le fichier de date)
          git add -A
          git diff-index --quiet HEAD || git commit -m "chore: Update jobs database and timestamp"
          git push